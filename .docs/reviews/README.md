# Risovach reviews

Review issue files are created only when independent review finds problems. Use one file per review run: `{scope}-{date}.md`.

Every finding has a stable ID, severity, category, evidence, location, impact, and remediation options. Reviewers do not modify source or rules files. Re-review appends a new run instead of overwriting history.
